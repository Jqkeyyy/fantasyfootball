# HANDOFF.md — current state

**Last updated:** 2026-08-16
**Last machine:** `MAYBE` (Windows).
**Last commit:** `d058527` on `main` (not yet pushed to origin — confirm before assuming remote is in sync). Mock draft keeper-source fix, manual Sleeper ADP baseline, and reach-temperature retune (see §1's newest entry and `docs/JOURNAL.md`'s matching entry for the real numbers).

**Task 13 close-out (worktree `worktree-ros-rankings-pipeline`, 2026-08-16, later still):** the 13-task ROS rankings pipeline plan (task 1.21) is done — real end-to-end run against both real leagues, three more real bugs found live and fixed, local commit made (not pushed — see §1's newest entry below for the real commit SHA and every detail).

This file is **state and pointers**, not design or history. `SPEC.md` and the addenda hold the design. Per-task evidence, implementation decisions, and gotchas live in `docs/JOURNAL.md` — read that when investigating a specific past task or bug, not every session. `docs/summary.md` is a third, newer reference: a standing narrative snapshot of current state and how things are going, meant to be read cover to cover rather than searched. Maintenance rules are at the bottom.

---

## 1. Where things stand

Current phase: **Phase 0 and Phase 2 are fully complete. Phase 1's tasks 1.15 and 1.20 are closed (not "done" — closed per `SPEC-ADDENDUM-04.md` §C's shipping rule: neither cleared its own real bar, B3/consensus ships instead, and that shipping choice is now live production code, not just a decision).** Task 1.21 (rest-of-season rankings pipeline) is now done — real end-to-end verification against both real leagues, closed out in an isolated worktree this session (see the newest §1 entry below). Task 1.22 (cold-start source schedule) remains open, queued per the addendum's own §F sequencing. Phase 3's model v2 Stages 1-3 are built/evaluated as real, honest exploratory work outside strict task order, Stage 4 investigated and parked (see below); **task 3.8 (in-season prediction logging, `SPEC-ADDENDUM-05.md` §B) is done, verified live** — the one urgent item from that addendum, tasks 3.9-3.11 (§C-§E) stay offseason, explicitly not started. The narrative below is kept in roughly chronological order across sessions, oldest first — read `docs/summary.md` instead if you want the current picture without the history (note: `docs/summary.md` is stale, predates several sessions of work below, and hasn't been refreshed).

Tasks 1.13 (Metrics module) and 1.17 (Evaluation report) are complete. Verified this session with a real 5-season walk-forward run: `uv run ffapp evaluate --seasons 2021 --seasons 2022 --seasons 2023 --seasons 2024 --seasons 2025` wrote `data/outputs/eval/20260813T183533Z/report.md` with populated metrics, real LightGBM feature importances, and real calibration curves. Both tasks' evidence is in `docs/JOURNAL.md`.

Phase 2 tasks 2.1–2.5, 2.7, and 2.9 were already complete — the simulation layer, start/sit assistant, DST model, and trade analyzer — built out of TASKS.md order on a prior work machine where Sleeper was network-blocked, confirmed with you first (see 2.1's entry in `docs/JOURNAL.md`).

Task 2.6 (Waiver wire) is complete — built this session, the first task on this machine (Sleeper reachable here) to actually need Sleeper's free-agent pool and transaction history. New `src/ffapp/tools/waivers.py` + `config.WaiverSettings`. Verified against the completed 2025 season (2026's real rosters are pre-draft leftovers, not usable — draft is Aug 22) and against real historical FAAB bidding data pulled from `bdff-chopped`'s actual transactions. Committed as `ea5cd70`, pushed.

Task 1.18 (Projection output pipeline) is complete — new `src/ffapp/models/predict.py` + `ffapp project --week N` CLI command, composing the availability/points/quantile models (tasks 1.14/1.15/1.16) into SPEC §6.2's `outputs/projections.parquet` schema. Verified for real against already-played weeks (2025 weeks 10/11 — 2026 has no nflverse release yet): 445/477 real projections, 0 nulls, full provenance, real upsert-by-`(season, week)` behavior confirmed with a second real run.

Task 1.19 (Streamlit weekly rankings page) is also complete — new `src/ffapp/app/weekly_rankings_page.py` + `src/ffapp/app/pages/2_Weekly_Rankings.py`, SPEC §15's second page. Verified live in a real Chrome session via `claude-in-chrome` (see §7 for the connection hiccup and how it resolved) against the real weeks 1.18 generated: position tabs, the floor/ceiling visible range, and both filters (position + real Sleeper-resolved availability) all confirmed working against real data, 0 console errors. Both 1.18 and 1.19 committed as `a75554a`, pushed. Full evidence for both tasks is in `docs/JOURNAL.md`.

Task 1.15's hyperparameter-search follow-up was attempted this session (a real 25-trial random search, `notebooks/tune_points_v1.py`/`.log`) and stopped partway through on your instruction ("stop here, deal with this later") — full result in `docs/JOURNAL.md`'s 1.15 entry and TASKS.md's own updated status line. Short version: tuning moves MAE past B2 (best config 5.005 vs B2's 5.107 on dev seasons 2018-2020) but **not one of 25 trials beat B2 on Spearman-within-position-week** — confirms the gap is architectural (SPEC §11.4's own diagnosis), not an undertuned model. The real confirmation run (`ffapp evaluate --seasons 2021 2022 2023 2024` with the tuned config wired into `points.py`) was deliberately not run — next step if resumed.

Task 2.8 (SOS and schedule grid) is complete — built later the same session, after 1.15's follow-up was paused. New `src/ffapp/tools/sos.py` (positional SOS, the schedule-grid pivot, matchup detail) + `src/ffapp/app/schedule_grid_page.py` (roster-highlight resolution, heatmap styling) + `src/ffapp/app/pages/3_Schedule_Grid.py`, SPEC §15's third page. A real design bug was caught live in a running Streamlit session (not a fixture) and fixed before shipping: the first confidence-threshold design reused SPEC §10.4's `k=250` shrinkage constant directly, which greyed out every real cell at every position — fixed with a per-position-group threshold computed from that group's own real data. Full account, including a second smaller Streamlit-rendering gotcha, in `docs/JOURNAL.md`'s 2.8 entry. Verified live end to end in a real Chrome session against `rogan-radinator-league`/2025 data, 0 console errors.

Task 2.10 (News pipeline) is complete — built later the same session, the biggest single Phase 2 task by SPEC's own hour estimate. New `src/ffapp/ingest/news.py` (real RSS ingestion — ESPN/CBS Sports/Yahoo NFL feeds, all three confirmed live before writing any code — plus the real Anthropic API structuring call, `claude-opus-5`, structured outputs, confidence/name-resolution routing to a real manual review queue) and `src/ffapp/tools/news_propagation.py` (the real second-order cascade SPEC calls "the valuable part": reuses task 1.7's own already-validated `add_vacated_shares` fed a synthetic early `Out` event, re-runs `models.predict.project_week` unmodified, surfaces the real handcuff in exactly the shape `tools.waivers.build_waiver_board` already expects). Two real bugs were caught only by running the cascade against real 2015-2025 data (a schema-mismatch concat and an `Int32`/`Int64` dtype mismatch, both invisible to same-shaped fixtures) and fixed with dedicated regression tests. Verified against a real historical case (Bucky Irving ruled out, Tampa Bay, real 2025 week 5): the recomputed vacated shares closely matched the real official values the existing pipeline separately produced, the real handcuff (Rachaad White) was correctly identified, and his recomputed projection moved substantially in the right direction, then flowed through the real waiver-board function end to end. **Not verified live against the real Anthropic API** — `ANTHROPIC_API_KEY` is empty in this machine's `.env` (confirmed before starting); the structuring call is tested against a mocked client only. Full account in `docs/JOURNAL.md`'s 2.10 entry.

Task 2.11 (Model health page) is complete — built later the same session, closing out **all of Phase 2**. New `src/ffapp/app/model_health_page.py` + `src/ffapp/app/pages/4_Model_Health.py`, SPEC §15's fourth page. Reads task 1.17's own already-built `report.md` directly and renders it via `st.markdown()` — deliberately no new chart, since 1.17 already made and documented the real decision to render "calibration plots" as a markdown table. Verified live in a real Chrome session against the real 2021-2025 evaluation report: every metric table, real feature importances, and real calibration tables rendered, 0 console errors; the report selector correctly showed only the one real directory (of three) that actually has a `report.md`. Full account in `docs/JOURNAL.md`'s 2.11 entry.

**Correction, same session: the "3.1 closed, stopped after Stage 2" entry below was a real misread on my part, not a genuine decision reversal by the project owner.** "yes move on to finishing the model" was agreement to *continue building the model*, not agreement to stop — I parsed it backwards, closed out 3.1, and only caught it when the project owner said plainly "I just want to continue the model." Corrected the same session: **3.1 is reopened, proceeding to Stage 3 (efficiency priors) on the project owner's explicit instruction**, despite Stage 1/Stage 2's own mixed real evidence (preserved honestly below, not erased) — CLAUDE.md's rule 6 caveat was raised and heard; the decision to proceed anyway is the project owner's to make and has now been made clearly twice.

**Stage 3 (efficiency priors) is built, evaluated, final-reviewed, fixed, and — after a real, verified ablation — simplified: the opponent-adjustment offset is gone, and the result is now clean across the board.** `shrunk_model` is now pure Empirical Bayes shrinkage (SPEC's own literal RULE, no offset, no trained model). Real result: beats `trailing_raw` (the real bar) on 3 of 4 outputs (`yards_per_target`, `td_rate_per_target`, `yards_per_carry`), loses on the fourth (`td_rate_per_carry`) — unchanged from before the offset was removed. **But it now also beats `league_mean` (the sanity floor) on all 4 of 4 outputs, up from 2 of 4** — the offset was specifically what dragged the two yards outputs down to a narrow loss against `league_mean`; removing it fixed that cleanly, not just by a wash. Full account of why the offset was removed (a real, position-broken-out ablation, re-verified on fixed code after you correctly caught that the first pass ran against a still-buggy commit) and the corrected numbers are in `docs/JOURNAL.md`'s newest entries. **Next task: Stage 4 (Monte Carlo recombination)**, the last stage of the 4-stage pipeline — same "after this plan" pattern Stage 1/2/3 have each already used (design/plan/build/evaluate, then report and stop for a go/no-go rather than auto-proceeding).

Model v2 Stage 1 is as far as it can reasonably go without a bigger architecture change; the real result is a genuine, unforced mixed outcome, your call on next steps. Model v2 Stage 1 (team environment, TASKS.md 3.1, SPEC §11.4) is built, went through a final whole-branch review that caught and fixed a real constraint-sign bug (see `docs/JOURNAL.md`'s correction entry), and — on your go-ahead — had the two most obviously-missing inputs (a team's own trailing play count and pass rate) added afterward to test the final review's own design-gap finding. **That addition moved almost nothing**: `team_plays` MAE improved by 0.01 (6.9822→6.9723), `pass_rate` MAE was flat within noise (0.0854→0.0855). Final real result (`docs/JOURNAL.md`'s newest entry has the full account, including CIs): `team_plays` — `team_env_model` mae=6.9723 (n=2718) vs `trailing_ewm_4` (the real bar) mae=7.4442 (n=2558) vs `league_mean` (sanity floor) mae=6.7236 (n=2718). `pass_rate` — `team_env_model` mae=0.0855 (n=2718) vs `trailing_ewm_4` mae=0.0887 (n=2558) vs `league_mean` mae=0.0850 (n=2718). **Stage 1 beats its real bar (`trailing_ewm_4`) comfortably on both targets — a genuine pass — but still loses to a plain league-wide average on both, and giving it the two features that should have closed that gap didn't.** The CIs for the model and `league_mean` overlap substantially on both targets, so "loses" is a real point estimate, not necessarily a statistically distinguishable one at this sample size. Not tuned to change this outcome at any point. Decide whether to proceed to Stage 2 (opportunity) anyway, treat this as evidence the decomposition's team-level stage has hit its real ceiling with these inputs, or stop here — same kind of open call as 1.15's own outcome, now with more evidence behind it than before. Before that: DST/K's replacement-level VOR fixed (streaming-aware, real historical data) and then DST/K excluded from the draft board entirely, per your own explicit workflow (see below) — both live-verified against the real 2026 board. See `docs/JOURNAL.md` for the full account. 1.15 remains a separate open item outside Phase 0/3 (paused mid-follow-up, see below); everything past Phase 0/1 is Phase 3 (SPEC §11.4's decomposed model v2 — Stage 1 above, Stages 2-4 not started — trade finder, empirical correlation estimation, season-long simulated rankings, route-data cost evaluation, public-readiness audit) — not started, not scoped in detail yet.

This session (2026-08-14): you kept feeling a DST/K appearing anywhere near draft-relevant was wrong even after the TE/K explanation from the prior session (below) — investigated rather than just capping the display. Built this league's real 2021-2025 historical DST/K scoring and simulated a simple streaming strategy (best available matchup each week): it beat even the single best *drafted* DST/K's own season total by ~1.5-2x, every one of the last 5 real seasons — confirms `tools.vor`'s "Nth-best preseason total" replacement level is the wrong proxy for a streamed position, exactly as SPEC §9.4 itself implies but the standard fixed-point baseline doesn't achieve on its own. New `tools/streaming.py` (empirical replacement level from real history, not a guessed constant) + a new `replacement_overrides` param on `tools.vor`. Verified live: best DST moved from board rank 44 (VOR +27.7) to 251 (VOR -97.4); best K from rank 67 (VOR +6.9) to 918 of 1076 (VOR -192.0); TE (McBride/Bowers, #8/#9) and everything else unchanged. Then, on a direct follow-up ("remove all of the kickers and defenses off the draft board"), added `draft.excluded_positions` (`config/settings.yml`, `["DST", "K"]`) — DST/K are now fully absent from the board (981 rows, positions exactly `QB/RB/TE/WR`), not just buried; the streaming-aware replacement level from the first fix is kept, not deleted, so it's still correct if that exclusion is ever turned off for a different league/scenario. Also added TASKS.md 3.7 (Phase 3, explicitly deferred): a weekly DST/K streamer tab for *that week's* projection, since you stream both positions rather than draft them — `models.dst.weekly_streamer_list` (task 2.7) already produces this for DST. Full evidence, including the real per-season streaming numbers and a separately-noticed-but-deliberately-unfixed real issue (kicker preseason point projections look implausibly low — SPEC §11.7 already says not to spend effort there), in `docs/JOURNAL.md`.

This session (2026-08-14, later): model v2 Stage 1 (team environment, SPEC §11.4's decomposed pipeline, TASKS.md 3.1) built end to end via a 6-task plan (design/plan docs under `.superpowers/sdd/2026-08-14-model-v2-stage1-team-environment/`) and verified for real against 2021-2025 data (Task 6). Tasks 1-5: new `opponent_neutral_pace_ewm_8` feature (with a real leakage bug caught and fixed in review before Task 2 even started — the brief's own same-week join design leaked the shared game's outcome; fixed to use the opponent's already-lagged pace), `models/team_environment.py`'s DST-style table reshape, the two baseline columns, the two monotonic-constrained LightGBM regressors, and derived `pass_attempts`/`rush_attempts` (always sum to `team_plays` exactly, by construction). Task 6's real result: **Stage 1 beats the design's own stated bar (`trailing_ewm_4`) on MAE for both `team_plays` (7.053 vs 7.463) and `pass_rate` (0.0861 vs 0.0886)** — a real pass, the first model in the decomposed-v2 line to clear its bar — **but does not beat the plain `league_mean` sanity floor on either target** (6.748 and 0.0852), which wasn't the pass/fail bar but is a real, honest wrinkle reported as-is, not tuned away. Full result and the note that the verification script had to be re-run directly (no process was actually executing it when this task resumed) in `docs/JOURNAL.md`'s new entry and `.superpowers/sdd/2026-08-14-model-v2-stage1-team-environment/task-6-report.md`. Per the design's own "after this plan" instruction, Stage 2 (opportunity) was not started — this is a real decision point for you, same pattern as 1.15's own outcome.

This session (2026-08-14, later still): the plan's own final whole-branch review (dispatched on the most capable model available per the subagent-driven-development skill) found and fixed a real, load-bearing bug the six per-task reviews had all missed: both of `team_plays`'s monotonic constraints (`neutral_pace_ewm_8`, `opponent_neutral_pace_ewm_8`) were registered as *increasing* when the real, independently-verified correlation with `plays` is negative for both (`neutral_pace_sec` is seconds-per-play — higher means slower, i.e. fewer plays) — the exact failure mode `models.points`'s own module docstring already documents guarding against, which this task's own brief inherited without that same check. Confirmed three separate ways before trusting it: the controller's own correlation check against real data before the fix was dispatched, the re-reviewer's independent trace of the fix's logic, and the controller's own independent re-run of the corrected script afterward, all landing on the identical numbers. Two more real cross-task issues fixed in the same pass (an internal lag-shift inconsistency between this table's trailing features and its own baselines, and a missing `season_type == "REG"` scope letting real postseason weeks leak into training/validation). Merged to `main` locally (worktree/branch cleaned up) once the corrected result was confirmed. The same review also surfaced a well-evidenced but out-of-scope design finding — Stage 1 was never given a team's own trailing target as an input — which was deliberately not auto-fixed (a scope change, not a bug) and instead came back to you as a recommendation; on your go-ahead, added `pass_rate_ewm_5` (new feature, mirrors the already-existing `plays_per_game_ewm_5` pattern) and wired both trailing-target features into the model, re-verifying the constraint signs empirically first (both confirmed increasing) rather than assuming. Re-ran the real evaluation a third time: the two new features moved almost nothing (`team_plays` MAE 6.9822→6.9723, `pass_rate` MAE 0.0854→0.0855, both within noise) — Stage 1 still clears its real bar comfortably on both targets and still loses to `league_mean` on both, by essentially the same margin as before. Full account, including the CI-overlap caveat on the `league_mean` comparison, in `docs/JOURNAL.md`'s newest entry.

This session (2026-08-14, later still): with model v2 Stage 1 at a real stopping point and real keepers being picked tomorrow (2026-08-15, moot-ing the keeper-marker follow-up noted below), pivoted to a genuine draft-day gap: `ffapp ingest rankings` (SPEC-ADDENDUM-03.md §E's own "morning of" runbook step) never existed as a real command, despite the runbook naming it literally. Built it — refreshes every per-stat rankings source, every ranks-only source, and ADP, with the same per-source graceful degradation `ffapp draft board` already established (promoted `draft.board`'s private `_fetch_point_sources`/`_fetch_rank_sources` to public rather than duplicating that logic, a second real caller). Verified live against real cached data: `4/4` point sources, `3/3` rank sources, ADP all refresh cleanly. Full account in `docs/JOURNAL.md`.

This session (2026-08-14, later still): model v2 Stage 2 (opportunity, SPEC §11.4, a five-task plan under `.superpowers/sdd/2026-08-14-model-v2-stage2-opportunity/`) built and evaluated for real against 2021-2025 data. New `models/opportunity.py` (`build_opportunity_table`/`add_opportunity_baselines`/`to_predictions_frame`) plus new position constants in `features/usage.py` — a plain arithmetic composition (a player's own trailing share of team volume × Stage 1's own predicted team volume), no trained model, per the design's own reasoning that a second trained model has no more reason to beat a naive baseline than Stage 1's did. The final task ran the real evaluation (`notebooks/evaluate_opportunity_v2_stage2.py`, new, scratch): re-ran Stage 1's own walk-forward backtest to capture its real out-of-sample predictions (never ground truth), then scored the composition against `trailing_raw` (the real bar) and `league_mean` (sanity floor) on `targets`/`carries`/`rz_touches`, both real input tables scoped to `season_type == "REG"` from the start. **Real result: the composition lost to `trailing_raw` on all three outputs** — `targets` mae=1.9076 vs 1.6471, `carries` mae=3.1310 vs 1.2930, `rz_touches` mae=5.6065 vs 0.8666 — and lost to the `league_mean` sanity floor on two of the three (`carries`, `rz_touches`); it only beat `league_mean` on `targets`. Not tuned to change this — there's nothing to tune at this stage (no hyperparameters, no model). A likely real explanation was found and reported for `rz_touches`'s especially large gap (`expected_rz_touches` multiplies a red-zone-touch *share* by *total* team plays rather than a red-zone-scoped volume — the real distributions back this out to a ~7x average overshoot; the formula matches `docs/design-model-v2-stage2-opportunity.md`'s own literal line, so this traces to the design itself, not an implementation slip) but `models/opportunity.py` was deliberately not touched to fix it, per the plan's own explicit instruction not to tune this stage unilaterally; `targets`/`carries` show no such units mismatch and still lost, plausibly because composing two independently-noisy predictions (a trailing share estimate and Stage 1's own team-volume prediction, which itself didn't beat `league_mean`) can amplify error relative to a single already-integrated trailing average. Full numbers, including real `n`/CIs for every predictor, in `docs/JOURNAL.md`'s newest entry. Per the plan's own "after this plan" instruction, not proceeded past automatically — this is a real decision point for you: fix the likely `rz_touches` units issue and re-evaluate, or treat this as evidence the naive-composition approach itself doesn't clear its bar at this stage, same kind of real, unforced outcome Stage 1 and task 1.15 both already produced.

**Superseded by a same-day final whole-branch review** (see §1's "Next task" above for the corrected numbers): the `team_plays` MAE figures in this paragraph were measured with an inverted monotonic-constraint sign on both pace features, caught only once every task's diff was reviewed together. The corrected re-run reaches the same qualitative conclusion (beats `trailing_ewm_4`, doesn't beat `league_mean`) with slightly better model MAE on both targets — see `docs/JOURNAL.md`'s correction entry.

**Superseded by a later same-day final whole-branch review** (of Stage 2, this time): the paragraph immediately above compared three predictors that were never actually scored on the same row set — `to_predictions_frame` reshaped Stage 2's full 2015-2025, every-position table, and `accuracy_metrics`'s own per-predictor null filter silently let `opportunity_composition` (null outside its own eligible positions and outside 2021-2025) be scored on a completely different, narrower population than `trailing_raw`/`league_mean` (non-null across the full history and every position). A second bug, found in the same review: ~33% of rows resolved to a *null* real target rather than 0 for a real active-roster player-week with no recorded stat line — contradicting this project's own established SPEC §11.1 convention. Both fixed (`build_opportunity_table` now fills the real usage counts to 0 before deriving `rz_touches`, matching `features/build.py`'s own precedent; the evaluation script now routes every predictor through `evaluation.backtest.run_walk_forward_backtest` + `BaselinePredictor`, exactly like Stage 1's own script, so every predictor for a given target is scored on the identical position-eligible, 2021-2025-scoped row set). **Corrected real result, same headline conclusion (composition still beats `trailing_raw` on zero of the three outputs) but different real numbers:** `targets` mae=1.9451 (n=22274) vs `trailing_raw` mae=1.4675 (n=33473) vs `league_mean` mae=2.3084 (n=36371); `carries` mae=3.1493 (n=9421) vs `trailing_raw` mae=2.2905 (n=16342) vs `league_mean` mae=4.2356 (n=17723); `rz_touches` mae=5.2547 (n=21898) vs `trailing_raw` mae=0.6308 (n=33473) vs `league_mean` mae=0.8138 (n=36371). **What changed qualitatively: composition now beats `league_mean` on `carries` too** (previously it lost, 3.1310 vs 1.8960 — now it wins, 3.1493 vs 4.2356), still losing to `league_mean` only on `rz_touches`. The `rz_touches` units-mismatch diagnostic (formula deliberately untouched) still holds and looks slightly worse in relative terms (~8.3x worse than `trailing_raw`, vs ~6.5x before). Full account, including CIs and why the fix-round's own manual approximation on `carries` didn't hold up against the real re-run, in `docs/JOURNAL.md`'s correction entry. Merged to local `main` once the corrected result was re-verified clean (fresh test suite run, scoped re-review of the fix, full 1032-test suite green on the merged result) — worktree/branch cleaned up, same close-out pattern as Stage 1.

This session (2026-08-15): picked up the open `rz_touches` decision on your instruction to "finish the whole model, whichever needs attention first." Fixed the real units mismatch (`expected_rz_touches` now uses a new `team_rz_touches_trailing_ewm_6` — a real team-level trailing red-zone-touch average built inside `models/opportunity.py` — instead of `predicted_team_plays`, since Stage 1 never modelled team red-zone volume and the true team-level total isn't persisted anywhere upstream). Hit a real environment problem first: `data/` was completely empty on this machine, so — with your explicit go-ahead — rebuilt the real Phase 1 pipeline via a subagent, scoped to exactly what the eval script needs (no Sleeper/rankings/ADP/draft-board touched, given real keepers were being locked the same day). Real re-run: `rz_touches` MAE dropped from 5.2547 to 0.8135 (confirms the fix is real), but it's still worse than `trailing_raw` (0.6310) and now only ties `league_mean` (0.8139) rather than beating it — `targets`/`carries` were unaffected (formula untouched) and barely moved. **Full corrected numbers and the final complete picture (composition loses to `trailing_raw` on all three real Stage 2 outputs, no more known bugs) in `docs/JOURNAL.md`'s newest correction entry.** You then asked "what do you propose as a fix" and, on hearing the blend proposal, said "yes go lets try it out" — built `models.opportunity.add_opportunity_blend` (a fixed 50/50 average of composition and `trailing_raw`, no trained weight) to test whether the two signals combine better than either alone. **Real result: the blend beats pure composition on all three outputs (a real, if modest, sign that composition does carry some genuine signal) but still loses to `trailing_raw` alone on all three** — `targets` 1.8312 vs 1.4685 (+24.7%), `carries` 3.1200 vs 2.2905 (+36.2%), `rz_touches` 0.8051 vs 0.6310 (+27.6%). Full account in `docs/JOURNAL.md`'s newest entry. My recommendation, now stronger, not weaker: don't proceed to Stage 3 on this foundation — CLAUDE.md rule 6 isn't cleared at either stage even after two real attempts to close the gap (the `rz_touches` fix and the blend), and Stage 3/4 are substantial builds that would compound rather than fix it. A fit-weighted blend is the one remaining untried probe if you want to fully exhaust this line, but the size of the gap means a well-fit weight would need to land close to 100% on `trailing_raw` to approach parity — functionally giving up on the composition, not a real win for it.

This session (2026-08-15, later still): asked directly for a genuine long-term recommendation ("what do you think will be best for long term, keep trying this or move on") rather than another incremental probe. **Closing decision: TASKS.md 3.1 stopped after Stage 2, not proceeding to Stage 3/4 — your explicit "yes move on."** The real reasoning: three genuinely independent architectures in this project (v1's single trained model, Stage 1, Stage 2 including its blend) have now all failed to clearly beat a dead-simple trailing average or league mean, each after a real, rigorous attempt. That convergence across unrelated approaches — not any one stage's result alone — is the actual evidence of a real ceiling in what this project's current data/features predict for these specific quantities, not an implementation gap Stage 3/4 would fix. Also weighed: the real Aug 22 draft is one week out, and Phase 2's tools are already built and useful regardless of this decision — a fourth model attempt was a worse use of the remaining time than either using what's already working or picking up a different Phase 3 item. `trailing_raw`/`league_mean` remain the real working baselines anywhere this project needs an opportunity/volume signal. Full reasoning in `docs/JOURNAL.md`'s closing entry; `TASKS.md` 3.1 checked off as stopped-by-decision, not silently abandoned.

**Superseded within the same session: this was a real misread, not the project owner's actual instruction.** "yes move on to finishing the model" meant continue building the full pipeline, not stop — caught when the project owner said directly "I just want to continue the model." 3.1 reopened, proceeding to Stage 3. The reasoning above (three architectures, all losing to trailing baselines) is still real and still recorded — it just isn't the basis for stopping anymore; the project owner has weighed it and wants to continue regardless.

This session (2026-08-15, later still): model v2 Stage 3 (efficiency priors, SPEC §11.4, a four-task plan under `.superpowers/sdd/2026-08-15-model-v2-stage3-efficiency-priors/`) built and evaluated for real against 2021-2025 data. Design: `docs/design-model-v2-stage3-efficiency-priors.md`. Tasks 1-3 built new `models/efficiency.py::build_efficiency_table` — SPEC's own literal RULE, no trained model: Empirical Bayes shrinkage (`prior_weight=50.0` for `yards_per_target`/`td_rate_per_target`, `80.0` for `yards_per_carry`/`td_rate_per_carry`, matching SPEC's own "~50 targets or ~80 carries" literally) toward the positional trailing mean, plus an additive opponent-adjustment offset built from task 1.9's already-lag-safe `def_adj_ypt_allowed_*`/`def_adj_td_rate_allowed_*` columns. Unlike Stage 2, this stage doesn't consume Stage 1's or Stage 2's own output at all — SPEC's own Stage 3 input list names only "player efficiency history" and "opponent adjusted rates," both already-real columns Stage 3 reads directly.
Task 4 ran the real evaluation: new `notebooks/evaluate_efficiency_v2_stage3.py` (scratch), routing all three predictors (`shrunk_model`, `trailing_raw`, `league_mean`) through `evaluation.backtest.run_walk_forward_backtest` + `BaselinePredictor` from the start — Stage 2's own final-review lesson (score every predictor on the identical row set) applied proactively here instead of retrofitted after a bug. **Real result — the first favorable outcome anywhere in this project's decomposed-v2 line: `shrunk_model` beats `trailing_raw` (the real bar) on 3 of the 4 outputs.** `yards_per_target`: 4.3366 vs 4.7230 (n=21296 vs 18923) — a real, distinguishable win, CIs don't overlap. `td_rate_per_target`: 0.0739 vs 0.0747 — wins on the point estimate, CIs overlap. `yards_per_carry`: 2.1894 vs 2.2653 (n=9359 vs 8262) — wins on the point estimate, CIs overlap narrowly. `td_rate_per_carry`: 0.0517 vs 0.0487 — the one real loss, reported plainly like every other unfavorable number in this project, not smoothed over because the other three are favorable. `shrunk_model` sits close to `league_mean` throughout (expected — a 50-80-touch prior weight shrinks hard toward the positional mean by design). The design doc's own caveat applies when reading the absolute MAE values: a single week's real per-touch rate is extremely noisy at low touch counts, so the *relative* ranking between predictors is what's being trusted, not the raw numbers — by that relative ranking this is a genuine net positive. Not tuned — no hyperparameters exist at this stage, `models/efficiency.py` untouched by task 4. Full numbers/CIs in `docs/JOURNAL.md`'s newest entry. Per this plan's own "after this plan" instruction, Stage 4 (Monte Carlo recombination, the last of the 4 stages) was not started — a real decision point for the project owner, same pattern Stage 1/2/3 have each already used.

**This session (2026-08-15, later still): the plan's own final whole-branch review (dispatched on the most capable model available, opus — same discipline Stage 1/2 already established) found real, previously-unmeasured problems in a branch whose per-task reviews had all approved it, then a fix round closed them out before merge.** Real bug (Important): `_combine_and_clamp`'s null-handling was asymmetric — `pl.col(adj_col).fill_null(0.0) - pl.col(league_avg_col).fill_null(0.0)` produced a spurious *nonzero* offset in the wrong direction whenever the opponent-adjustment value itself was missing but the group's league average wasn't (the normal real case, not a rare edge case) — the function's own docstring promised graceful degradation to "no adjustment," and it didn't deliver that. Fixed by gating explicitly on `adj_col.is_null()`; a new test proves the fix. Also hardened `_add_raw_ingredients`'s `cum_sum().shift(1)` correctness with a defensive re-sort — it had been implicitly relying on row order surviving polars joins that don't officially guarantee order preservation (confirmed not currently manifesting as a real bug on real data, but fragile). **The genuinely new finding: a paired-bootstrap ablation the branch's own evaluation never ran.** Comparing `expected_*` (shrinkage + opponent-adjustment offset) against `_shrunk_*` (shrinkage alone), same rows: the offset is a measured net negative on `yards_per_target` (+0.0163 mean absolute-error increase, CI [+0.0077, +0.0246], excludes zero) and `yards_per_carry` (+0.0321, CI [+0.0200, +0.0433], excludes zero), and statistically neutral on both `td_rate_*` outputs. A separate ablation of the shrinkage step alone against `league_mean` shows shrinkage genuinely beats `league_mean` on **all four** outputs (CIs exclude zero) — meaning the shrinkage math itself is real, measured signal; it's specifically the additive opponent-adjustment offset dragging the two yards outputs back down to (narrowly) losing against `league_mean`. Also recorded: a real common-support check (restricting to rows where all three predictors have a value, closing a subtler "identical row set" overstatement the eval script's own comments made) confirms the headline `trailing_raw` comparison holds either way — this makes the result *more* defensible, not less. **Deliberately left open, not decided in the fix round: whether the opponent-adjustment offset stays, gets reworked, or gets dropped** — a real project-owner-level design call, not something to resolve unilaterally, especially with real evidence on both sides (real signal on 2 outputs, real harm on 2 others). Re-reviewed clean (all 7 findings addressed, one Minor docstring-wording nit parked, non-blocking), then merged to local `main` (`11d875c`) once the merged result's own 1055-test suite passed clean — worktree/branch cleaned up, same close-out pattern as Stage 1/2.

**This session (2026-08-15, later still): `SPEC-ADDENDUM-04.md` (rest-of-season weekly rankings) added to the repo, read in full, wired into `CLAUDE.md`'s reading order, and three new tasks (1.20, 1.21, 1.22) queued in `TASKS.md`.** The addendum disputes task 1.15's own prior "architectural gap" conclusion as premature (a hyperparameter search over one fixed feature set/target/loss can't distinguish an architecture problem from a feature/target/measurement problem) and specifies five cheap real diagnostics (§A) to run before 1.15 can honestly stay closed. Also proposes an anchored-residual architecture as a fallback (§B, now task 1.20), a rest-of-season rankings pipeline (§D, now task 1.21), and a cold-start source schedule (§E, now task 1.22). Explicitly not started this session — sequenced after Stage 4's own investigation per the addendum's own §F ("the right long-term architecture and the wrong thing to start four weeks before a season," about decomposed v2 broadly).

**This session (2026-08-15, later still): model v2 Stage 4 (Monte Carlo recombination) investigated end to end — real design work, a real prototype, a real calibration failure found and partially diagnosed — then deliberately parked as offseason work on your explicit instruction. No Stage 4 code was added to `src/`; every artifact was scratch, deleted after each check.** Architecture settled: a `MarginalSource` interface both v1's existing quantile model and Stage 4 would implement, plugging into `sim/week.py`'s already-built, already-tested Gaussian-copula machinery rather than duplicating it (not yet built — the investigation never reached the refactor). A systematic audit (your own request, not incidental) of every non-zero real scoring key in each league's `scoring_settings` against real Stage 1-3 coverage found two real structural gaps: **QB passing production has zero decomposed-pipeline coverage anywhere** (closing it properly needs two more full stages, offseason-sized), and **`rec` (receptions) is also uncovered** (Stage 2 gives targets, Stage 3 gives yards/TD rates, nothing gives a real catch rate — the raw column already exists in `player_week_stats.parquet`, just never surfaced as an output). Decision: **Stage 4 scoped to non-QB-passing production (RB/WR/TE) plus a new `catch_rate_per_target` output added to Stage 3 (reopened, gated on the same three-way ablation every other Stage 3 output already cleared — never actually built) and a cheap `fum_lost` rate; 2-point conversions treated as zero, documented; v1 continues to cover QB/K/DST in production** (a legitimate per-position configuration per the addendum's own §A.5 precedent, not a gap). Marginal-construction approach settled through four real refinements: an empirical, transformed-scale (logit/log), full-residual-vector bootstrap from real historical player-weeks, scaled per-row by an n-specific expected scale rather than stratified — confirmed as the right layering (raw samples never touch `sim/week.py`'s copula; only a per-player quantile grid of total points does, matching `mixture_with_p_active`'s own output shape). **Real empirical validation before writing any design doc, per your own requirement, surfaced real problems the design alone wouldn't have caught:** the hypothesized `1/√n` scaling law was tested against real 2015-2022 data and rejected (only 2 of 13 real position/quantity combinations were genuinely log-linear) — replaced with an interpolated lookup table, per your own specified fallback; TE rushing (71 real train rows) was confirmed too thin and shape-mismatched against RB rushing to pool, needing a parametric fallback instead. **The real calibration test (chronological 2015-2022 train / 2023-2025 test, checked against v1's own established 2.1pp bar) failed badly on the first prototype — systematic over-dispersion at every position, RB off by 9-14pp, WR by 14-20pp, TE by 15-29pp on the 50%/80% intervals.** One real implementation bug was found and fixed in the same pass (the pool had been standardized by a single global constant rather than each row's own n-specific scale) but barely moved the numbers (RB 57.9%→59.0%, WR 68.3%→69.9%, TE 77.6%→78.7% on the 50% interval), ruling it out as the dominant cause. **A second, more targeted diagnostic (per-component coverage, plus a parallel no-rescaling run) — run on your own precise direction — ruled out the scale-lookup machinery entirely (rescaled vs raw nearly identical everywhere) and found the real pattern is genuinely mixed, not matching any of the three hypothesized clean categories:** opportunity (targets/carries) is over-dispersed everywhere (RB targets 76.1% actual vs 50% nominal), while `catch_rate` is severely under-dispersed in the opposite direction (RB 0.8% vs 50% nominal, TE 4.3%) — a real, distinct, unresolved problem, plausibly `catch_rate`'s own discreteness at low real target counts (a continuous logit-normal residual model likely smooths over common real extreme low-volume outcomes), not yet verified further. **Decision, made by you: park it here.** The named likely real fix — compound sampling (draw N real opportunities, then draw N per-touch outcomes jointly, sum, letting `√N` scaling emerge by construction, expecting to need a game-level random effect to correct for the resulting under-dispersion) — is a rewrite-scale change, not attempted this session. A simpler alternative (sampling total scored points per historical row as a whole) was considered and explicitly rejected on real grounds: it can't respond to a real role change, can't apply different league scoring across this project's two real leagues, and collapses to roughly B2. **Next session moves to `SPEC-ADDENDUM-04.md` §A's five diagnostics instead** — full account, every real number, in `docs/JOURNAL.md`'s newest entry.

**This session (2026-08-15, earlier): you asked what I'd recommend for the open Step-2 question, I said drop it, and before deciding you asked three sharp verification questions that caught a real gap in the evidence before any code changed.** (1) Was the offset-harm ablation run before or after the null-adjustment-fill fix — a sign bug in null handling and a finding that the offset "pushes the wrong direction" could be the same bug. (2) Was the league average computed per position group or pooled — pooled would hide a real TE effect. (3) Was the offset itself shrunk by real sample size, or applied at full weight regardless. **Checked all three against the actual code and git history rather than assuming: (1) confirmed dirty — the ablation ran on `b6593dd`, the fix landed after in `b6fd1a1`, so it needed a real re-run on fixed code, not just a caveat. (2) confirmed correctly per-group — the pooling issue didn't exist; what was missing was printing the per-position breakdown `accuracy_metrics` already computed internally. (3) confirmed real but nuanced — the underlying `def_adj_*` values are already shrunk by task 1.8's own real sample size (SPEC §10.4's `n_plays/(n_plays+250)`), but Stage 3's own combine step applied that value at full weight with no further discount of its own.** Re-ran the ablation on the fixed code with a real per-position breakdown: `yards_per_target` hurts RB (+0.019, CI excludes zero) and WR (+0.014, CI excludes zero), TE is indistinguishable from zero (+0.017, wider CI from a smaller sample, not a smaller effect); `yards_per_carry` hurts QB (+0.075 — the single largest harm measured anywhere) and RB (+0.013); both `td_rate_*` outputs remained neutral everywhere. **On your explicit instruction — drop the offset everywhere including TE (its point estimate sits between two confirmed-harmful positions; keeping it there would be selecting on noise) — removed `_combine_and_clamp`/`_select_by_position`/`_select_group` and the `_OutputSpec` fields that only served them; `expected_<output>` is now exactly the shrunk estimate, no clamping needed either (provably bounded without it once the offset — the only thing that could push a value out of range — was gone).** Re-ran the real evaluation on the simplified model: **a clean, unambiguous improvement, not a wash** — `shrunk_model` still beats `trailing_raw` on 3 of 4 outputs (unchanged) and now beats `league_mean` on all 4 of 4 (up from 2 of 4) — `yards_per_target` 4.3203 vs `league_mean` 4.3312 (previously lost, 4.3366 vs 4.3312) and `yards_per_carry` 2.1573 vs `league_mean` 2.1801 (previously lost, 2.1894 vs 2.1801) both flipped to real wins. The one real, unchanged loss remains `td_rate_per_carry` vs `trailing_raw`. Recorded in the design doc as a real finding about this specific additive functional form (QB rushing treated identically to RB via one shared term, on top of task 1.8's own estimate applied at full weight), not a finding that opponent adjustment itself is worthless — a real, named option for a future rework, not attempted here. Full account, including the exact per-position ablation numbers, in `docs/JOURNAL.md`'s newest entries. Full suite re-verified clean (1049 tests — down from 1055, the 7 offset/clamp tests removed, 1 new test added proving `expected_* == _shrunk_*` directly).

This session (2026-08-13): SPEC-ADDENDUM-03.md housekeeping; task 0.16 (static HTML export) and task 0.15 (mobile draft page, with a new `ffapp draft live --replay/--stop` command) both built and verified live; **ESPN and FFToday rankings sources fixed**; **two new ranks-only sources added, FootballGuys (526 real players) and DraftSharks (125 real players, `Crawl-delay: 10` respected)** — `draft/board.py` generalised to a `_RANK_SOURCE_FETCHERS` dict; **keepers restored to the board, visibly marked** — prototyped safely against the dummy CSV first, then built into `draft/board.py` for real once you confirmed it, with a distinct amber highlight + 🔒 marker in both the Streamlit board page and the static HTML export (a real debugging detour here: the Streamlit page briefly *appeared* unstyled after this change, turned out to be a stale long-running dev-server process with a cached pre-edit import, not a real bug — restarting fixed it, and that lesson paid off again below); **a real DST cross-source matching bug found and fixed** — you flagged TE (McBride/Bowers) and DST/K ranking unusually high, which turned out to be two very different things: the TE/K part is real, intended VOR behaviour (all 7 sources agree, dispersion lower than comparable WRs, real ADP confirms the model is finding genuine positional-scarcity value ADP-driven consensus doesn't price in — not a bug), but DST was a real bug: every one of the 32 real teams was spelled a different way by each of 5 sources (ESPN "Texans D/ST", FantasySharks/FFToday "Houston Texans", CBS "Houston", ADP "Houston Defense"), so cross-source matching silently never aggregated any DST at all — every DST board row was really one source's single, unchecked opinion (`n_sources=1`, `dispersion=0.0`, `adp=None` on every one). Fixed with a new canonical team-name table in `projections/aggregate.py` (32 teams × the 4 confirmed real spellings each); DST now properly aggregates (`n_sources=6` per team, real dispersion, real ADP). Board grew 854 → 1141 → 1077 players over the session (rankings expansion added players, then the DST fix collapsed ~64 duplicate unaggregated DST rows into 32 real ones); **Draft Board page row cap added** — you asked for the board to stop rendering all 1000+ rows by default (genuinely slow to paint) and cap at 400 unless a button/checkbox is used to see more. New `cap_rows` in `app/draft_board_page.py`, wired into the sidebar as a checkbox that only appears once the filtered board actually exceeds 1000 rows; verified live (dev server restarted first, same lesson as above) showing "400 of 1077" capped, then "1077 of 1077" once checked. Full evidence for all of the above in `docs/JOURNAL.md`. `docs/summary.md` was not refreshed this session — still describes the state as of the prior close-out; it doesn't mention any of this session's work yet.

**This session (2026-08-16): `SPEC-ADDENDUM-04.md` §A's five diagnostics run for real against 2018-2020 dev seasons, then task 1.20 (anchored residual) built, evaluated, and closed, then the resulting shipping decision made real in production code.** §A: A.1 confirmed Spearman was being measured on the wrong population (all rows, not startable — narrowed the gap to B2 by about a third, didn't close it); A.2 confirmed B2's own value was missing as a feature (adding it closed about another third); A.3 confirmed real prediction compression (std(pred)/std(actual) ≈ 0.57, every position); A.4 ruled out the training objective (a `lambdarank` run didn't fix the compression); A.5 found the failure concentrated at QB/TE, not uniform. None cleared 1.15's bar outright, so task 1.20 (§B's own fallback) was built next: new `models/residual.py`, composing `B2 + residual_model(features)` (floor = B2 by construction) with a per-position blend weight fit to maximize Spearman on dev seasons. Real result on 2021-2024: a small, non-significant Spearman edge at RB/WR only (no CI excludes zero), and — after a 20-seed robustness check and a fair common-support restriction — a near-wash on real lineup regret. **The decisive test:** fitting the blend weight to directly minimize dev-season regret instead found a combo that looked dramatically better on dev data (28.0 vs 39.0 pts/week) but did not replicate on held-out seasons (33.2 vs 32.4) — a clean overfitting result, the most reusable finding in this line of work. **Closing decision: ship B3/consensus per `SPEC-ADDENDUM-04.md` §C** — three independent real tests all agree neither the direct nor anchored-residual line beats B2 robustly, and B3 beats both on real lineup regret, repeatedly, under a fair comparison.
Then the shipping decision was made real, not just decided: new `config.ModelSettings.projection_source` (`anchored | direct | baseline_b2 | consensus_b3`, real default `consensus_b3`), `models.predict.project_week` rewritten to dispatch on it (a real row-alignment bug caught and fixed before it shipped, not by a test — see `docs/JOURNAL.md`), `ffapp project`/the Model Health page both wired end to end. Along the way, a real, separate blocking question surfaced (`SPEC-ADDENDUM-04.md` §D: "B3 gives point estimates, but start/sit and the trade analyzer need distributions") and was resolved by a real coverage check, not assumed: an empirical B3-error-based spread cleared task 1.16's own 5-point bar at every position; the first interim implementation (v1's own quantile spread recentered on B3) badly failed (up to 24 percentage points off nominal on the 80% interval) and was replaced before shipping. Verified live twice against real 2025 week 10 data (445 real projections both times); the Model Health page's new section could not be verified in a live browser this session (Chrome extension not connected on this machine) — still owed. Full account, every real number, in `docs/JOURNAL.md`'s newest entries.

**This session (2026-08-16): `SPEC-ADDENDUM-05.md` (consensus as anchor, and the offseason model plan) added, read in full, wired into `CLAUDE.md`'s reading order, tasks 3.8-3.11 queued in `TASKS.md`. Task 3.8 (in-season prediction logging, §B) built, evaluated, and verified live — the one urgent item in that addendum, everything else (§C-§E, tasks 3.9-3.11) stays offseason, not started.** New `tools/prediction_log.py` + `ffapp log week`/`log backfill`/`log check-sources`. **Two real bugs found only by live verification against real 2025 week-10 data, not by unit tests.** (1) The six non-`fantasypros` rankings sources return real season-long totals through the exact scoring path the draft board correctly relies on, not weekly numbers (a real QB showed `espn`=243.7/`cbs`=213.3 alongside `fantasypros`/`b3_mean` agreeing ~15-18) — the first design silently averaged both into one "weekly" dispersion figure. Verified this did **not** touch the already-shipped `b3_mean`/`consensus_b3` or the B3 historical archive (separate real pipeline, confirmed by code tracing per your own "report both before proceeding"). Fixed with two real namespaces (`weekly_source_points`/`season_source_points`), `dispersion`/`n_sources` from the one real weekly source only, a new `season_dispersion`/`n_season_sources` from the other six as its own legitimate rest-of-season signal (kept, not dropped — feeds `SPEC-ADDENDUM-04.md` §D), plus `check_sources`' new `season_trend` (declining/flat/insufficient_data, resolves itself by ~Week 4). (2) A real GitHub API `403 rate limit exceeded` hit live — the fix for bug (1) had `_fetch_fantasypros` fetching FantasyPros' full paginated commit list twice per run (once via `baselines.fetch_b3_for_week`, once more just to hash the payload); fixed to fetch once and build `b3` via `baselines.add_b3_fp_weekly_consensus` directly. **Final live verification** (`ffapp log week --season 2025 --week 10 --run-label tuesday`, using same-day cache to avoid the still-cooling rate limit): 445 rows, 0/7 sources failed, every source including `fantasypros` (previously always null) carries a real `payload_sha256`. Two other quick checks, both resolved with no code change needed: the weather re-ingest is confirmed live at 0% null across all 94,422 rows/2015-2025 (previously 100% null per `SPEC-ADDENDUM-04.md` §A); task 0.16 (draft HTML export) was already verified live in an earlier session. **Discovered, not fixed, real and out of scope tonight:** the "rankings gitignore exception" this task's own §B.3 cites as precedent doesn't actually exist yet — `data/raw/rankings/` (194 real files) is still caught by the blanket `data/` ignore; only the new prediction-log path got a real exception this session. 17 new tests, full suite 1111 tests green, `mypy`/`ruff` clean. Full account, every real number, in `docs/JOURNAL.md`'s newest entry. Next: `SPEC-ADDENDUM-04.md` §D (the ROS pipeline), per your own explicit "next work is ADDENDUM-04 §D" instruction — no modelling from ADDENDUM-05 during the season.

**This session (2026-08-16, later still): local mock draft simulator built and verified live — not a numbered SPEC/TASKS.md task, direct request: manually re-creating every draft in the real Sleeper app to practice strategy was too slow to "mass practice."** New `draft/mock.py` (bot logic + orchestration), `app/mock_draft_page.py` (pure rendering helpers), `app/pages/6_Mock_Draft.py` (Streamlit glue). Deliberately built almost entirely out of already-existing, already-tested pieces rather than a new pipeline: the real board CSV (`ffapp draft board`, already has VOR/tier) as the player pool, `tools.adp.keeper_sleeper_ids`/`keeper_join_keys` (same functions `draft.board.build_draft_board` itself uses) to pre-seed real locked keepers onto their real owner's roster, `draft.pick_order.pick_owner` (trade-aware — this league trades picks) for turn order, and `draft.live`'s own already-tested `starting_lineup_gaps`/`positional_run` reused directly for the bots' need/run signals. Bots sample (not argmax) from `exp(-|adp-pick_no|/temperature) × need_multiplier × run_multiplier` — asked for and confirmed with you before building (ADP+randomness+team needs+reaction to recent picks, "somewhat complex to mimic an actual person's draft").

**Real gap found and closed before building any of the above: Sleeper has no public or documented ADP anywhere** — confirmed by reading `docs.sleeper.com` directly (nothing there) and by inspecting `/v1/players/nfl`'s own `search_rank` field live against a real bench player (real ADP ~465, `search_rank` was `9999999`, the deep-bench sentinel — a materially different number, not usable as ADP). The real source, found via a live Chrome session against the user's own real league's "Players" tab switched to a season view: `api.sleeper.com/projections/nfl/<season>` (a different, undocumented host from this project's existing `BASE_URL`, no `/v1`) — each player's `stats.adp_ppr` is Sleeper's own real ADP. Confirmed genuinely public (no session cookie, no auth header, a bare `curl` still returns 200 with real data — 3300 rows, every fantasy position including DEF, one call, no pagination). New `ingest.sleeper.fetch_sleeper_adp`/`normalize_sleeper_adp`, `ingest.sleeper._get` extended to accept an already-absolute URL (this is on a different host than every other Sleeper call in this module) rather than duplicating the fetch/cache machinery. A FantasyPros-based alternative (their consensus ADP page has a real per-platform `SLEEPER` column) was investigated first and rejected: the full player list is gated behind a login wall (only 5 rows return unauthenticated, confirmed live via the page's own embedded JSON), and creating an account or logging in on the user's behalf is prohibited.

**Two real bugs found only by live verification in the browser, not by the 24 new unit tests:** (1) a stale long-running Streamlit dev-server process didn't pick up the newly-added `sleeper.fetch_sleeper_adp` function after a hot-reload (`AttributeError: module has no attribute`) — same known quirk this project has hit before (task 0.13's session note); fixed by a full process restart, not a code change. (2) `sleeper.fetch_users` for the primary league had never been called by any existing code path (`draft.board` only ever needed `fetch_rosters`) and wasn't cached yet — a real, expected `OfflineCacheMiss`, not a logic bug; warmed live (`fetch_users(..., offline=False)`). **Verified live end-to-end after both fixes**: real keepers (Jonathan Taylor) pre-seeded onto the user's own roster before pick 1, real bot picks using real teammates' real Sleeper display names (TheeBeastly, lovedamunch, ThomasR7, ...) in real ADP order, the position filter and player-select-and-draft flow both working, the draft correctly resolving bots up to the user's own next real trade-aware pick number each time, 0 browser console errors across multiple picks. 33 new tests (4 for `fetch_sleeper_adp`/`normalize_sleeper_adp`, 20 for `draft.mock`'s bot-decision math/state machine, 9 for `app.mock_draft_page`'s pure rendering helpers), full suite 1144 green (up from 1111), `mypy --strict`/`ruff` clean.

**This session (2026-08-16, later still): three real follow-ups to the mock draft, each a direct request against the running feature. Full account, every real number, in `docs/JOURNAL.md`'s newest entry — summarized here.** (1) **Sleeper's own `roster.keepers` field is confirmed wrong for this league** (you spotted Jahmyr Gibbs, never actually kept, marked as one) — replaced with a hand-curated `config/keepers_rogan-radinator-league_2026.yml` (your real 10 keepers) resolved by new `draft/keepers.py`, wired into both the real Draft Board and the mock draft's own grid; `build_draft_board` prefers this override when present, falls back to the old Sleeper-field logic otherwise. Verified live: Gibbs `is_keeper=False`, all 10 real keepers correctly flagged with the right owner. (2) **Bot picks now prefer your real hand-exported `rankings/ADP_Sleeper.xlsx`** over the live Sleeper ADP API pull — new `ingest.manual_rankings.fetch_manual_sleeper_adp`/`normalize_manual_sleeper_adp` (657 rows, 356 with real ADP; no `Position` column in this export, so it joins by player name alone). Effective ADP priority is now: manual file → live API → board's own ADP → `overall_rank`. (3) **A real test draft put Brock Bowers (TE) 1st overall — "would never happen."** `REACH_TEMPERATURE` (governs how far off-ADP a bot reach can go) had no empirical basis at `6.0` — simulated 500 pick-1 draws at the old value and confirmed the complaint was real (Gibbs won only ~20% of the time, real outliers appeared). Retuned to `1.5` after simulating several flat/schedule candidates against real board+keeper+manual-ADP data; a single flat low temperature reproduces "tight early, loose late" on its own (real ADP data is dense near rank 1, sparse deep in the draft — no pick-number-dependent schedule needed). **1000 real simulated pick-1 draws at `1.5`: Gibbs 68.0%, Chase 16.1%, Nacua 7.5%, McCaffrey 4.3%, Smith-Njigba 2.8%, zero Bowers-caliber outliers** — matching your own described shape exactly, while round-10/15 picks still show real, wide spread. **Separately found, not caused by this session's own code:** this machine's `data/raw/` (rosters/drafts/ADP/user caches) was completely empty mid-session — `data/outputs/` untouched. Re-warmed live (6 real Sleeper GETs); `init_mock_draft` now runs fully offline again. Root cause not investigated. New regression test pins the outlier-suppression behavior directly (`test_pick_weights_at_pick_1_makes_a_real_off_adp_outlier_implausible`). Full suite 1229 green, `mypy`/`ruff` clean, no live browser re-check this round (Chrome extension disconnected mid-session) — verified instead via direct calls to the real pure functions against the real re-warmed cache.

**This session (2026-08-16, later still, in an isolated worktree, `worktree-ros-rankings-pipeline`): Task 13 — the ROS rankings pipeline's own real end-to-end verification and close-out. Task 1.21 (rest-of-season rankings pipeline) is now done, not just built.** Real calibration constants (Task 5, already committed to `config/ros_calibration.yml` before this task started): within-player week-to-week correlation `rho` — QB=0.3446, RB=0.3972, TE=0.3512, WR=0.3803; injury-duration `recovery_prob` — QB=0.259, RB=0.4761, TE=0.4722, WR=0.5027. This task ran the real pipeline end to end against both real leagues for the first time: `ffapp project --season 2025 --from-week 10 --through-week 17 --league <slug> --no-offline` then `ffapp rankings ros --league <slug>`, for both `rogan-radinator-league` (10-team, real `playoff_week_start=15`) and `bdff-chopped` (18-team, real `playoff_week_start=0` — not yet configured on Sleeper for the 2026 season, so its `playoff_weeks_value` column is legitimately `0.0` for every row, not a bug).

**Real board-difference confirmation (the required "different `LeagueFormat`s produce materially different `vor_ros`" check), actual numbers observed:** the identical real player, RB `00-0041027`, scored `vor_ros=185.24` at rank 1 in the 10-team `rogan-radinator-league` board vs `vor_ros=165.93` at rank 2 in the 18-team `bdff-chopped` board — a real ~19-point swing from the two leagues' different replacement levels/free-agent pools, confirming `LeagueFormat` is genuinely driving the fixed-point computation rather than being ignored. `rogan-radinator-league` rank 1 overall: RB `00-0041027`, `ros_points=224.71`, `expected_games=8.0`, `playoff_weeks_value=98.68` (a real, separate, smaller column than `ros_points`, checked against this league specifically per this task's own correction note). A second consecutive real run showed real non-null `rank_change` movement (+1/-1 at several ranks), proving the `latest.parquet` diff mechanism against two real runs.

**Three more real bugs found live during this task's own verification, none caught by any existing unit test, all fixed with real/updated tests (full account, every number, in `docs/JOURNAL.md`'s newest entry):** (1) `cli.py`'s `rankings_ros_command` fed `sim.injury.build_hazard_features` the wrong injuries table (post-normalisation `interim/injuries.parquet`, missing the `gsis_id` column that function's own raw-nflverse contract needs) — fixed to call `nflverse.fetch_injuries` like its sibling `rosters_table`/`snap_counts` reads two lines below it already did. (2) `sim.injury.fit_hazard_model` crashed on a real `ValueError: Input X contains NaN` — 157 of 96,081 real hazard-grid rows have a genuinely null `age`; fixed with median imputation ahead of `StandardScaler`. (3) The big one: a real, deliberately-null `mean`/quantile grid for a `(player, week)` with no empirical error-quantile bucket yet (an intentional, already-documented "never guess, leave it null" design from tasks 7/8) was propagating into a `NaN` season total for that whole player in `tools.ros_aggregate.aggregate_ros`, and `NaN`'s default sort-first behaviour put 49 of 622 real `rogan-radinator-league` players at ranks 1-49 — ahead of every real projection — before the fix. Fixed by excluding a null-`mean` week from that player's own weekly marginals rather than fabricating a value for it.

**Fix-round correction, found by independent review, same session:** bug (3)'s original fix excluded a null-`mean` week from `totals`/points correctly, but left `expected_games` computed over the full week list regardless — a player missing one week's projection still had that week silently counted as an expected game, understating real points-per-game with no visible flag. Fixed with a `has_projection` mask in `tools/ros_aggregate.py`, gating `expected_games` by the same per-`(week, player)` condition the points exclusion already uses. Real regression-test values: a one-null-week player now shows `expected_games=2.0` exactly (was `3.0`) vs a fully-populated 3-week case's `expected_games=3.0`. Also added the dedicated regression tests all three original fixes were missing: `test_aggregate_ros_null_week_excluded_from_points_and_expected_games` (`tests/test_tools_ros_aggregate.py`) and `test_fit_hazard_model_handles_null_age_without_crashing` (`tests/test_sim_injury.py`). Full suite now 1156 tests, still green.

**Streamlit page (`6_ROS_Rankings.py`) not verified in a live browser this session** — same real `claude-in-chrome` connection quirk as before (§7 below), `list_connected_browsers` returned `[]`. Fell back to a real `HTTP 200` on `/ROS_Rankings` while the dev server was live, plus the page's own pure-function test coverage (`tests/test_app_ros_rankings_page.py`) — genuinely not the same as a rendered-in-browser check, still owed.

**Real, small follow-up gap, not built this session:** no `--all-leagues` flag exists on `ffapp project`/`ffapp rankings ros` (only `ffapp cache warm --all-leagues`/`ffapp scoring validate --all-leagues` have it) — `SPEC-ADDENDUM-04.md` §D.4's own literal Tuesday-runbook text names it for the two new ROS rows; `SPEC.md` §16.1 was updated with the real, working per-league invocation instead of inventing a flag that doesn't exist. A natural next-task candidate whenever the Tuesday runbook actually needs to run unattended across both real leagues.

Full test suite (`uv run pytest`), `ruff check`, `ruff format --check`, and `mypy src/` all clean after every fix. Committed locally on this worktree's own branch (`worktree-ros-rankings-pipeline`) — **not pushed**, per this task's own explicit instruction; push/merge is a separate decision for after the plan's final whole-branch review. `git status` clean after the commit.

**This session (2026-08-16, later still, back in the isolated worktree): the final whole-branch review found real cross-cutting bugs, one severe, all fixed in a single final fix wave.** Read the finished system against the REAL output artifacts Task 13's own e2e run had left on disk (not just the diff) — found: **48% of a real ranked board (299/622 players) were silently defaulting to "perfectly healthy" (`p_active=1.0`, `p_miss=0.0`) whenever a player had no anchor-week feature/hazard row**, all 299 landing at the maximum `expected_games`, 36 of them in the top 50 — defeating §D.2's own `p_play[w]` requirement for half the real deliverable, and doing the opposite of this project's own "honest null, never guess" convention. Fixed with real positional base-rate fallbacks (`sim.injury.positional_base_rate` reused directly, plus a new `models.baselines.positional_availability_base_rate` mirroring its shape) and, when even that's unavailable, honest exclusion (reusing the existing `has_projection` mask) rather than a fabricated value. Also fixed in the same wave: the ROS Rankings Streamlit page showed no player name/ID anywhere (fixed — `player_name` now carried through and leads the table); a real-mean/null-quantile row could still slip past the null guard and reproduce the NaN-sorts-to-rank-1 bug class (guard widened to require all quantile columns non-null too); `rankings ros` hardcoded `offline=True` with no CLI flag (added, matching `project`'s own precedent); three cheap cleanups (a side-effect config write replaced with the already-public side-effect-free `season_source_trend`, misleading `--from-week` help text corrected, a docstring note that cross-player correlation is architecturally inert for every column this pipeline emits). Deliberately **not** touched, per explicit scope discipline in the plan's own last allowed fix wave: a real but more invasive anchor-week double-counting issue in `ros_points` (recorded as a known follow-up, not silently shipped as if it didn't exist), `actuals_to_date`'s existing "missing row = 0" design choice, and a low-severity join-ordering fragility in already-shipped `ros_shape.py`. Full suite 1165 tests, `ruff`/`mypy` clean. Scoped re-review confirmed all 5 fixes correct end-to-end, no new breakage. Committed locally, still not pushed.

**Blocking on me (the human), not the agent:**

- [x] GitHub remote created and pushed
- [x] Sleeper username supplied (`Maybe17`)
- [x] Primary league chosen — `rogan-radinator-league`
- [x] Ranking sources: **seven now live and contributing** — FantasyPros, ESPN, FantasySharks, CBS Sports, FFToday (ESPN/FFToday fixed this session), plus FootballGuys and DraftSharks (added this session). See `docs/JOURNAL.md` for per-source depth/coverage detail.
- [x] Draft date and slot — slot 3 of 10, Aug 22 2026. League trades draft picks and is a 1-keeper league — see task 0.11's entry.
- [ ] Waiver type/budget — captured automatically via `--discover` (`rogan-radinator-league`: type 1, $100 FAAB). Resolved unless you want to override it.

---

## 3. In progress

Nothing is mid-implementation. Everything asked for this session (2026-08-16: ADDENDUM-04 §A diagnostics, task 1.20 build/evaluate/close, shipping B3 for real, the distribution-wrapper fix) is fully done, verified for real (twice, for the projection pipeline — see §1), and about to be committed. Other genuinely open threads, already named above, not repeated in full here:

- **Model Health page's new "live projection source" section was NOT verified in a live browser this session** — `claude-in-chrome`'s extension wasn't connected on this machine (needs the real Chrome browser specifically signed in, not the machine's apparent default — a previously-documented quirk, see §7). Confirmed instead via a raw HTTP 200 on the page's own route while the dev server was running, and via the underlying pure functions' own real test coverage (`tests/test_app_model_health_page.py`). **Verify live in a real Chrome session next time this machine has it properly signed in** — low risk (thin `st.markdown` glue over already-tested functions) but not yet actually seen rendered.
- **1.15 and 1.20 are closed, not open, and 1.21 (rest-of-season rankings pipeline) is now done too** — see §1's newest entry. Task 1.22 (cold-start source schedule) is the real next item in this line, per `SPEC-ADDENDUM-04.md` §F's own sequencing (though the draft, Aug 22, still outranks all of it).
- **Model v2 Stage 3 (efficiency priors) — done, final-reviewed, fixed, simplified (opponent-adjustment offset removed on real ablation evidence), re-evaluated, merged, and pushed.** `shrunk_model` (pure Empirical Bayes shrinkage now, no offset) beats `trailing_raw` on `yards_per_target`/`td_rate_per_target`/`yards_per_carry`, loses on `td_rate_per_carry`, and beats `league_mean` on all four outputs. Full numbers in `docs/JOURNAL.md`.
- **Model v2 Stage 4 (Monte Carlo recombination) — investigated 2026-08-15 (design, real prototype, real calibration test), then deliberately parked as offseason work. Not built; no `src/` code exists.** Real calibration coverage test against v1's own 2.1pp bar failed (systematic over-dispersion, 9-29pp off depending on position/interval); a targeted per-component diagnostic found a real, mixed, unresolved pattern (opportunity over-dispersed, `catch_rate` severely under-dispersed) with a plausible but unverified explanation (catch rate's own discreteness at low real target counts). The named likely fix (compound sampling, drawing N opportunities then N per-touch outcomes and summing) is a rewrite, not attempted. Full account — every real number, every ruled-out hypothesis — in `docs/JOURNAL.md`. **Do not resume this cold without reading that entry first.** Offseason work per `SPEC-ADDENDUM-04.md` §F, not a near-term item.
- Stage 1 (loses to `league_mean` on both targets) and Stage 2 (loses to `trailing_raw` on all three targets) remain mixed/unfavorable, recorded honestly, not erased by Stage 3's better result.
- **Keepers still don't show a marker on the mobile draft page (task 0.15) or in the live draft assistant (`draft.live`)** — deliberately not built. Real keepers were picked 2026-08-15, a week before the real draft opens (Aug 22); by the time the mobile page/live assistant matter, Sleeper's own real pick events will have already resolved every keeper into a real pick, so `available_pool` naturally excludes them from "best available" by then regardless. Revisit only if a future keeper-locking flow doesn't work the same way.
- **`data/outputs/draft_board_2026_dummy.csv` still exists** — a safe test copy from an earlier session's keeper exploration, gitignored, harmless to leave or delete.
- **`structure_news_item`** (task 2.10) has never been exercised against a real Anthropic API call — `ANTHROPIC_API_KEY` is empty in this machine's `.env`. The natural first step once a key is supplied is one real live smoke-test call, not a code change.
- **`docs/summary.md` is stale** — predates several sessions of work (ADDENDUM-03, all of model v2 Stages 1-4, ADDENDUM-04, 1.15/1.20's closure, shipping B3). Refresh when next touched, per its own "rewrite in place when stale" convention.

This session's own work, in order: (1) reconciled `TASKS.md` against an orphaned commit (message `"g"`) from a prior session that had landed real, tested work for 1.13/1.17 without updating either tracking doc; (2) restructured this file — moved §2/§4/§5 to `docs/JOURNAL.md` verbatim, this file keeps only state and the rebuild/environment reference below; (3) built, verified, committed, and pushed task 2.6 (Waiver wire); (4) built, verified, committed, and pushed tasks 1.18 (Projection output pipeline) and 1.19 (Streamlit weekly rankings page) together, the latter verified live in a real browser session after resolving a real Chrome/Vivaldi extension-connection issue (see §7); (5) later session, same day: committed the doc reconciliation from (1)-(4) that had been left uncommitted (`1beeb5e`); (6) ran and stopped 1.15's hyperparameter-search follow-up (see above); (7) built, verified live, and documented task 2.8 (SOS and schedule grid), including finding and fixing a real confidence-threshold design bug live in the browser before calling it done; (8) built, verified against real 2015-2025 data, and documented task 2.10 (News pipeline), including finding and fixing two real schema/dtype bugs the propagation cascade's own fixture tests hadn't caught, before calling it done; (9) built, verified live, and documented task 2.11 (Model health page), closing out Phase 2 in full; (10) committed everything from (6)-(9) as five separate, logically-scoped commits, then discovered via `git status` (not assumed) that the prior session's `1beeb5e` had never actually reached `origin` despite its own header claiming otherwise, and pushed all six real commits together; (11) wrote and committed `docs/summary.md`, a new standing narrative overview of the whole project.

---

## 6. Rebuilding `data/` from empty

Full rationale for each step is in `docs/JOURNAL.md`'s per-task entries (§2). Run in this order on an unrestricted network — see ADDENDUM-02 §B.

```bash
uv run ffapp ingest sleeper --season 2026 --discover --no-offline
uv run ffapp cache warm --season 2026 --all-leagues --no-offline
uv run ffapp cache status
```

Player-id crosswalk (task 0.3 — no CLI command yet, only the function):

```bash
uv run python -c "from ffapp.ingest import nflverse; from ffapp.config import load_settings; nflverse.fetch_player_ids(offline=False, settings=load_settings())"
uv run ffapp ids check --season 2026
```

The six canonical interim tables (task 1.1, 2015–2025 — 2026 has no nflverse release until games are played) through the full feature pipeline (tasks 1.2–1.9) are built by a Python script, not a CLI command. `docs/JOURNAL.md` task 1.9's entry has the exact real call sequence ending in `features_build.build_player_week_features(...)` → `data/features/player_week_features.parquet`; task 1.1's entry lists the six interim tables and task 1.3/1.4's entries cover schedule/weather/injuries. **On a network where Sleeper is blocked**, `ids.mapping.build_players_dim` can skip `layer_sleeper_ids` — see JOURNAL task 1.1's entry for the exact workaround and its real coverage numbers.

Baselines B0–B3 (task 1.10): B0–B2 are pure functions over `player_week_features.parquet`. B3 needs a live FantasyPros weekly-archive git-history mining pass — `notebooks/materialize_b3_historical.py` (real, permanent script, not scratch) does this for real, writing `data/interim/fp_weekly_consensus.parquet` and `data/interim/b3_predictions.parquet`:

```bash
uv run python notebooks/materialize_b3_historical.py
```

~100 unauthenticated GitHub requests for the real 2021-2025 range this script covers (60/hour rate limit — idempotent, safe to re-launch if rate-limited partway through). **`data/interim/b3_predictions.parquet` is now a real production dependency**, not just an evaluation aid — `ffapp project`'s real shipped default (`projection_source: consensus_b3`, §C's own shipping rule) needs it to build the live quantile spread; `ffapp project` exits with a clear, named error if it's missing.

Rankings/ADP/traded-picks (tasks 0.7, 0.11) — no CLI command; see JOURNAL for the exact fetch calls, then:

```bash
uv run ffapp draft board --season 2026
```

Evaluation report (tasks 1.12–1.17):

```bash
uv run ffapp evaluate --seasons 2021 --seasons 2022 --seasons 2023 --seasons 2024 --seasons 2025
```

`--seasons` must repeat per value — typer's `Option` doesn't support `nargs=-1` (space-separated values after one flag), unlike SPEC's own literal example invocation. See JOURNAL's gotchas for the full explanation.

DST model (task 2.7): a narrower real materialisation, no Sleeper needed — only `pbp`/`team_stats`/`player_stats`/`schedules`/`snap_counts` (raw) and `schedule`/`team_week_context`/`weather` (interim). See JOURNAL task 2.7's entry for the exact script.

Weekly projections (task 1.18) — writes `data/outputs/projections.parquet`, upserted by `(season, week)`:

```bash
uv run ffapp project --season 2025 --week 10 --no-offline
```

Needs that `(season, week)`'s own row universe to already exist in `player_week_features.parquet` — for a genuinely live/current week this means whatever nflverse has published so far (2026 has none yet). The real default `projection_source` is `consensus_b3` (`SPEC-ADDENDUM-04.md` §C) — needs a real, live player-id crosswalk fetch (`--no-offline` the first time) plus `data/interim/b3_predictions.parquet` already built (see the B3 baseline step above); exits with a clear error naming whichever is missing.

Schedule Grid page (task 2.8): no CLI command, same no-CLI precedent as every other UI-only page. Needs `interim/schedule.parquet`, `interim/defense_position_allowed.parquet`, and `features/player_week_features.parquet` to already exist — all three already built by the steps above, nothing task-2.8-specific to run first.

News pipeline (task 2.10): no CLI command. `ingest.news.fetch_rss_feed` needs `--no-offline` for a real fetch (writes `data/raw/news/<source>.xml`); `structure_news_item` needs a real `ANTHROPIC_API_KEY` in `.env` (empty on this machine — see §1). `tools.news_propagation.propagate_ruled_out_player` needs `interim/injuries.parquet`, `interim/team_week_context.parquet`, and `features/player_week_features.parquet` — all three already built by the steps above.

Model Health page (task 2.11): no CLI command. Needs at least one real `data/outputs/eval/<timestamp>/report.md` to exist — built by the evaluation report command above (task 1.12-1.17); nothing task-2.11-specific to run first.

Mock Draft page (not a numbered task, `draft.mock`): no CLI command. Needs the real board CSV (`ffapp draft board`, above) already built, plus two real Sleeper calls no earlier task needs — `sleeper.fetch_users(league_id, offline=False)` (this league's owner display names; never cached by anything else, a real `OfflineCacheMiss` the first time) and `sleeper.fetch_sleeper_adp(season, offline=False)` (Sleeper's own real ADP, `api.sleeper.com`, see §1's newest entry for how this was found). Both are one-time-per-session warms, not needed again until they go stale.

Rest-of-season rankings (task 1.21) — writes `data/outputs/<league_slug>/projections_ros.parquet` and `data/outputs/<league_slug>/rankings_ros/latest.parquet`:

```bash
uv run ffapp project --season 2025 --week 10 --from-week 10 --through-week 17 --league <slug> --no-offline
uv run ffapp rankings ros --league <slug> --season 2025 --offline
```

`--week` is still required by the CLI even in `--from-week`/`--through-week` range mode (a real, small wiring gap — the value is unused in that code path; pass any real week, e.g. the same value as `--from-week`). `rankings ros`'s `--season` must match whatever season `project` was actually run for — it does NOT default from `--from-week`, it defaults to `settings.seasons.current` (2026, which has no real data yet), so omitting `--season` against a 2025 run fails with "no real current-week row." `rankings ros` now has a real `--offline/--no-offline` flag (added in the final fix wave, matching `project`'s own) — pass `--no-offline` to force a fresh Sleeper roster fetch for the current free-agent pool rather than whatever's cached. Needs `config/ros_calibration.yml` already populated (Task 5's `notebooks/estimate_ros_calibration.py`, already committed) and, for a league never seen on this machine before, its real Sleeper rosters cached (`ffapp cache warm --season 2026 --all-leagues --no-offline` warms every real league's rosters regardless of season argument). No real `--all-leagues` flag exists on either `project` or `rankings ros` yet — run once per real league slug.

**Not reproducible by re-running the above:** `data/raw/rankings/` (time-sensitive projections/ADP — committed to git as CLAUDE.md's stated exception; do not delete), `data/raw/sleeper/traded_picks_*.json`/`rosters_*.json` (keeper locks, time-sensitive), odds snapshots if the paid API is enabled.

**2026 nflverse data doesn't exist yet.** Every `fetch_*` call for season 2026 404s until real games are played.

**Must be supplied by hand on a new machine:** `.env` (copy from `.env.example`; `FFAPP_OFFLINE=1`/`FFAPP_CACHE_STRICT=1` are sane defaults).

**Windows console gotcha, not a code bug:** `print()`-ing a polars DataFrame crashes with `UnicodeEncodeError` under `cp1252` stdout encoding. Use `.to_dicts()`/plain prints in one-off scripts run here.

---

## 7. Environment notes

**Python:** 3.13.5 (3.11+ satisfied), managed with `uv`. `uv.lock` is committed.
**Offline default:** `FFAPP_OFFLINE=1` in `.env`. Cache warming and league discovery need `--no-offline` explicitly.
**Dependencies:** `lxml`(+stubs), `scikit-learn` (`numpy` pinned `<2.5` — see JOURNAL task 0.10's gotcha), `scipy`, `streamlit`(+`pandas-stubs`), `lightgbm`, `pandas`, `pulp`, `anthropic`, `defusedxml`(+dev-only `types-defusedxml`). Full per-task rationale for each addition is in `docs/JOURNAL.md`.

**Network reachability, most recent confirmation per location:**

| Location | Sleeper | nflverse / GitHub | PyPI | Rankings sites |
|---|---|---|---|---|
| `Maybe` (this session, 2026-08-13) | ✓ (`api.sleeper.app` 200) | not re-checked this session, previously ✓ | not re-checked, previously ✓ | not re-checked this session |
| Work, `ITHQ-172-26-LT1` (2026-08-13) | ✗ — Cisco Umbrella content filter blocks Sleeper specifically (confirmed: real `block.sse.cisco.com` redirect, not a TLS/cert issue) | ✓ | ✓ | ✓ (FantasyPros, FantasyFootballCalculator, CBS Sports confirmed) |
| Home | ? | ? | ? | ? |

If a new machine shows up, add a row rather than overwriting existing ones — don't assume `Maybe` again without checking the actual hostname.

**`claude-in-chrome` browser verification on this machine (`Maybe`) needs the actual Chrome browser, not Vivaldi (the machine's apparent default) or any other Chromium-based browser, and the Claude extension must be installed *and signed in* there specifically.** `list_connected_browsers`/`tabs_context_mcp` both return empty until that's true — closing Vivaldi and opening Chrome alone isn't sufficient if the extension isn't logged into the same claude.ai account this session uses. Once signed in, connection works immediately (confirmed live again this session, task 0.16's own real browser verification, same as task 1.19's).

**`claude-in-chrome`'s `navigate` tool refuses `file://` URLs by default** ("Can't interact with browser-internal or unparseable URLs") — confirmed live this session verifying task 0.16's static export. Workaround: serve the directory over `127.0.0.1` with `python -m http.server <port>` and navigate to that instead; doesn't compromise a "no network calls" test since `read_network_requests` still shows exactly what the page itself requests.

**Subagent-driven-development process lesson from this session (Stage 2's real evaluation, twice):** a subagent that starts a real long-running background process itself (e.g. `uv run python notebooks/evaluate_*.py`, ~15-80 min) will sometimes wrongly assume it will receive an external "task notification" the way a top-level controller does, and stop prematurely to "wait" for one that never arrives — it's a subagent, not the controller; there is no such signal for it. Twice this session (Task 5's implementer, and the final-review fix implementer) had to be resumed with an explicit correction telling them to poll/block on their own backgrounded process themselves. If a subagent dispatch involves starting something genuinely long-running, say so explicitly in the dispatch and state directly that no external notification is coming — don't assume the instruction is obvious. Separately, the final-review fix implementer was cut off mid-task by an **account-level API monthly spend limit** (not a bug) after completing all the real substantive work but before writing its own report or committing — the controller verified the resulting diff directly (tests/ruff/mypy) and committed it manually rather than losing the work. If this recurs, check for uncommitted worktree changes before assuming a dispatch failed cleanly.

---

## Maintenance rules

At the end of every session, before committing:

1. Update the header (date, machine, commit).
2. Move newly completed tasks' evidence into `docs/JOURNAL.md` §2, and tick the matching box in `TASKS.md`.
3. Update §1 with the next task and its first concrete step.
4. Record anything half-finished in §3, precisely enough to resume cold.
5. Add any implementation decision to `docs/JOURNAL.md` §4 and any gotcha to §5.
6. Keep §6 and §7 accurate as commands and reachability change.

Delete sections that have gone stale rather than letting them rot. A handoff file nobody trusts is worse than none.
